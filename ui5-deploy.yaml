# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: supplieropenitems
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://emfiori.everestspices.in:50001/
          client: '100'
        app:
          name: ZSUPPLIERITEMS
          description: Supplier items fiori app
          package: ZFIORI4
          transport: EMFK900403
        exclude:
          - /test/

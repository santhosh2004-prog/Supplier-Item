<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:table="sap.ui.table"
>
    <!-- ═══ Accepted qty panel : shown in place of Line items after the
         "Accepted qty" module row (MATERIALS (MM) group) is pressed. Calls
         AcceptSet for the current Supplier over the searched From/To Date
         range (Fromdate/Todate — AcceptSet's own filter names, keyed by
         Lifnr only, no Bukrs), same stat-tile + table layout as Rejected
         qty — no chart. ═══ -->
    <VBox visible="{recon>/mdShowAcceptedQty}" class="mdPanelAnimate">
        <FlexBox
            class="mdStatTilesRow"
            wrap="Wrap"
        >
            <HBox
                justifyContent="Start"
                class="mdSidebarReopenRow"
                visible="{= !${recon>/mdLeftPanelOpen} }"
            >
                <Button
                    id="mdLeftPanelMenuButtonAcceptedQty"
                    icon="sap-icon://navigation-right-arrow"
                    type="Transparent"
                    tooltip="Open"
                    class="mdSidebarGroupTitle"
                    press=".onMdLeftPanelMenuPress"
                />
            </HBox>
            <!-- <VBox
                visible="{= ${recon>/acceptedQtyBusy} || ${recon>/acceptedQtyItems}.length > 0 }"
                class="mdStatTile mdStatTileNeutral"
            >
                <Text
                    text="Accepted Qty"
                    class="panelSubTitle mdNoMargin textLeft"
                />
            </VBox> -->
            <VBox
                visible="{= ${recon>/acceptedQtyBusy} || ${recon>/acceptedQtyItems}.length > 0 }"
                class="mdStatTile mdStatTileBlue"
            >
                <HBox
                    justifyContent="SpaceBetween"
                    alignItems="Center"
                >
                    <HBox alignItems="Center" class="mdStatTileContent">
                        <Text
                            text="Total Accepted Qty :"
                            class="panelSubTitle mdNoMargin textLeft"
                        />
                        <Text
                            text="{ path: 'recon>/acceptedQtyTotal', formatter: '.formatBalanceValue' }"
                            class="panelSubValue mdNoMargin openingBalanceValue"
                        />
                    </HBox>
                    <core:Icon
                        src="sap-icon://trend-up"
                        class="mdStatIcon mdStatIconBlue"
                    />
                </HBox>
            </VBox>
            <VBox
                visible="{= ${recon>/acceptedQtyBusy} || ${recon>/acceptedQtyItems}.length > 0 }"
                class="mdStatTile mdStatTileGreen"
            >
                <HBox
                    justifyContent="SpaceBetween"
                    alignItems="Center"
                >
                    <HBox alignItems="Center" class="mdStatTileContent">
                        <Text
                            text="Total Lots Accepted :"
                            class="panelSubTitle mdNoMargin textLeft"
                        />
                        <Text
                            text="{= ${recon>/acceptedQtyItems}.length }"
                            class="panelSubValue mdNoMargin openingBalanceValue"
                        />
                    </HBox>
                    <core:Icon
                        src="sap-icon://list"
                        class="mdStatIcon mdStatIconGreen"
                    />
                </HBox>
            </VBox>
             <HBox class="mdExportBtns mdLineItemsToolBtns">
                <Button
                    icon="sap-icon://action-settings"
                    text="Column Settings"
                    type="Transparent"
                    tooltip="Column Settings"
                    press=".onLineItemsColumnSettingsPress"
                >
                    <customData>
                        <core:CustomData key="tableId" value="acceptedQtyTable" />
                    </customData>
                </Button>
                <Button
                    icon="sap-icon://table-view"
                    text="Select Layout"
                    type="Transparent"
                    tooltip="Select Layout"
                    press=".onLineItemsLayoutDialogPress"
                >
                    <customData>
                        <core:CustomData key="tableId" value="acceptedQtyTable" />
                    </customData>
                </Button>
            </HBox>
        </FlexBox>

        <IllustratedMessage
            visible="{= !${recon>/acceptedQtyBusy} &amp;&amp; ${recon>/acceptedQtyItems}.length === 0 }"
            illustrationType="sapIllus-NoData"
            illustrationSize="Spot"
            title="No Accepted Qty Data"
            description="{= 'No accepted lots found for ' + (${recon>/mdSupplierName} || ${recon>/mdLifnr}) + '.' }"
            class="mdNoDataIllustration"
        />

        <!-- <HBox
            justifyContent="SpaceBetween"
            alignItems="Center"
            class="mdRightHeaderRow"
            visible="{= ${recon>/acceptedQtyBusy} || ${recon>/acceptedQtyItems}.length > 0 }"
        >
            <Text
                text="{= 'Accepted qty (' + ${recon>/acceptedQtyItems}.length + ' records)' }"
                class="panelSubTitle mdLineItemsTitle mdNoMargin"
            />
            <HBox class="mdExportBtns mdLineItemsToolBtns">
                <Button
                    icon="sap-icon://action-settings"
                    text="Column Settings"
                    type="Transparent"
                    tooltip="Column Settings"
                    press=".onLineItemsColumnSettingsPress"
                >
                    <customData>
                        <core:CustomData key="tableId" value="acceptedQtyTable" />
                    </customData>
                </Button>
                <Button
                    icon="sap-icon://table-view"
                    text="Select Layout"
                    type="Transparent"
                    tooltip="Select Layout"
                    press=".onLineItemsLayoutDialogPress"
                >
                    <customData>
                        <core:CustomData key="tableId" value="acceptedQtyTable" />
                    </customData>
                </Button>
            </HBox>
        </HBox> -->

        <table:Table
            id="acceptedQtyTable"
            width="100%"
            rowHeight="24"
            columnHeaderHeight="26"
            rows="{recon>/acceptedQtyItems}"
            busy="{recon>/acceptedQtyBusy}"
            busyIndicatorDelay="0"
            visible="{= ${recon>/acceptedQtyBusy} || ${recon>/acceptedQtyItems}.length > 0 }"
            selectionMode="None"
            enableColumnReordering="false"
            visibleRowCountMode="Auto"
            minAutoRowCount="13"
            noData="No accepted lots found."
            class="mdLineItemsTable mdGridTable"
        >
            <table:columns>
                <table:Column width="16%" sortProperty="Prueflos" filterProperty="Prueflos">
                    <table:label>
                        <Label text="Lot Number" design="Bold" />
                    </table:label>
                    <table:template>
                        <Link text="{recon>Prueflos}" press=".onLotsReceivedLotNumberPress" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col1" />
                    </table:customData>
                </table:Column>
                <table:Column width="10%" sortProperty="Werks" filterProperty="Werks">
                    <table:label>
                        <Label text="Plant" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{recon>Werks}" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col2" />
                    </table:customData>
                </table:Column>
                <table:Column width="14%" sortProperty="Enstehdat" filterProperty="Enstehdat">
                    <table:label>
                        <Label text="Date" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{ path: 'recon>Enstehdat', formatter: '.formatOpenItemDate' }" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col3" />
                    </table:customData>
                </table:Column>
                <table:Column width="16%" sortProperty="Matnr" filterProperty="Matnr">
                    <table:label>
                        <Label text="Material" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{recon>Matnr}" wrapping="false" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col4" />
                    </table:customData>
                </table:Column>
                <table:Column width="14%" sortProperty="Charg" filterProperty="Charg">
                    <table:label>
                        <Label text="Batch" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{recon>Charg}" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col6" />
                    </table:customData>
                </table:Column>
                <table:Column width="10%" sortProperty="Vcode" filterProperty="Vcode">
                    <table:label>
                        <Label text="Code" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{recon>Vcode}" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col7" />
                    </table:customData>
                </table:Column>
                <table:Column
                    width="10%"
                    hAlign="End"
                    sortProperty="Qkennzahl"
                    filterProperty="Qkennzahl"
                >
                    <table:label>
                        <Label text="Acceptance %" design="Bold" />
                    </table:label>
                    <table:template>
                        <Text text="{recon>Qkennzahl}" textAlign="End" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col11" />
                    </table:customData>
                </table:Column>
                <table:Column
                    width="14%"
                    hAlign="End"
                    sortProperty="Losmenge"
                    filterProperty="Losmenge"
                >
                    <table:label>
                        <Label text="Lot Quantity" design="Bold" />
                    </table:label>
                    <table:template>
                        <ObjectNumber number="{ path: 'recon>Losmenge', formatter: '.formatAmount' }" />
                    </table:template>
                    <table:customData>
                        <core:CustomData key="colId" value="col8" />
                    </table:customData>
                </table:Column>
            </table:columns>
        </table:Table>
    </VBox>
</core:FragmentDefinition>
